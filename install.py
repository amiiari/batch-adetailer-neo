# No additional pip dependencies required — this extension drives the installed
# ADetailer extension and uses only packages already bundled with Forge Neo.
print("[Batch ADetailer] No extra dependencies to install.")
